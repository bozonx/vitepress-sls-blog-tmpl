title: site
body: |
  title: ""
  description: ""
  siteTitle: ""

  donate:
    url: "${PROPS.siteUrl}/${PROPS.lang}/${PROPS.theme.donateUrl}"
    icon: "${PROPS.theme.donateIcon}"
    postDonateCall: "If you like this project, please support it financially."

  # Configuration of the publisher for JSON-LD
  publisher:
    name: "Your Blog Name"
    url: "https://yourblog.com"
    logo: "https://yourblog.com/logo.png"

  authors:
    - id: "ivan-k"
      name: "Ivan K"
      descr: "some descr"
      image: "https://....webp"
      links:
        - type: "youtube"
          url: "https://www.youtube.com/..."
          title: "YouTube channel ..."
  footer:
    message: "Copying allowed only with a link to the source."
    copyright: "Copyright © 2024-present ... ."
    links:
     - text: "${PROPS.t.links.aboutBlog}"
       href: "page/about-blog"
  topBar:
    # Show donate button in top bar. Default is false.
    donate: true
    # Show guide button in top bar. Default is false.
    guide: true
    links:
      - text: "${PROPS.t.links.donate}"
        href: "${PROPS.siteUrl}/${PROPS.lang}/${PROPS.theme.donateUrl}"
        icon: "${PROPS.theme.donateIcon}"
        iconClass: donate-icon
        mobileToo: true
      - text: "${PROPS.t.links.guide}"
        href: "${PROPS.siteUrl}/${PROPS.lang}/${PROPS.theme.docUrl}"
        icon: "${PROPS.theme.docIcon}"
  sideBar:
    # Show recent posts in side bar. Default is false.
    recent: true
    # Show archive in side bar. Default is false.
    archive: true
    # Show authors in side bar. Default is false.
    authors: true
    # Show donate button in the bottom of the side bar. Default is false.
    donate: true
    # Show guide in the bottom of the side bar. Default is false.
    guide: true
    # Show rss feed in the bottom of the side bar. Default is false.
    rssFeed: true
    # Show atom feed in the bottom of the side bar. Default is false.
    atomFeed: true
    links:
      - text: "${PROPS.t.links.recent}"
        href: "${PROPS.theme.recentBaseUrl}/1"
        icon: "${PROPS.theme.recentIcon}"
      - text: "${PROPS.t.links.byDate}"
        href: "${PROPS.theme.archiveBaseUrl}"
        icon: "${PROPS.theme.byDateIcon}"
      - text: "${PROPS.t.links.authors}"
        href: "${PROPS.theme.allAuthorBaseUrl}"
        icon: "${PROPS.theme.authorsIcon}"
    bottomLinks:
      - text: "${PROPS.t.links.donate}"
        href: "${PROPS.siteUrl}/${PROPS.lang}/${PROPS.theme.donateUrl}"
        icon: "${PROPS.theme.donateIcon}"
        mobile: true
      - text: "${PROPS.t.links.guide}"
        href: "${PROPS.siteUrl}/${PROPS.lang}/${PROPS.theme.docUrl}"
        icon: "${PROPS.theme.docIcon}"
        mobile: true
      - { header: "${PROPS.t.links.links}" }
      - text: "${PROPS.t.links.ourYoutubeChannel}"
        href: "https://www.youtube.com/@p"
        icon: "${PROPS.theme.youtubeIcon}"
      - text: "${PROPS.t.links.ourTelegramChannel}"
        href: "https://t.me/"
        icon: "${PROPS.theme.telegramIcon}"
      - text: "${PROPS.t.links.ourTelegramChat}"
        href: "https://t.me/"
        icon: "${PROPS.theme.chatIcon}"
      - text: "${PROPS.t.links.weInSocialMedia}"
        href: "${PROPS.siteUrl}/${PROPS.lang}/${PROPS.theme.linksUrl}"
        icon: "${PROPS.theme.socialLinksIcon}"
      - text: "RSS feed"
        href: "${PROPS.hostname}/feed-${PROPS.lang}.rss"
        icon: "${PROPS.theme.rssIcon}"
      - text: "Atom feed"
        href: "${PROPS.hostname}/feed-${PROPS.lang}.atom"
        icon: "${PROPS.theme.atomIcon}"
